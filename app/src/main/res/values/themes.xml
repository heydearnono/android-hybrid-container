<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 自己画 TopAppBar，所以用 NoActionBar，避免出现两条标题栏。 -->
    <style name="Theme.Base" parent="@android:style/Theme.Material.Light.NoActionBar" />
</resources>
