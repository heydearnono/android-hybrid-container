<?xml version="1.0" encoding="utf-8"?>
<lint>
    <!--
      按路径关掉，而不是在 convention plugin 里全局 disable：ObsoleteSdkInt 对代码里
      过时的 Build.VERSION.SDK_INT 判断是有用的，只有 adaptive icon 这一处例外
      （去掉 -v26 会让 AAPT 找不到资源，已实测）。
    -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/res/mipmap-anydpi-v26" />
    </issue>
</lint>
