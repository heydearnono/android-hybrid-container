# ADR-NNNN：{决策标题}

> 复制本模板到 `docs/adr/NNNN-决策slug.md`（NNNN 从 0001 递增），删掉 `>` 引导说明。
> ADR 是**不可变记录**：决策变了写新的 ADR 并把旧的标为「已被 ADR-XXXX 取代」，不要原地改结论。

| | |
|---|---|
| 编号 | ADR-NNNN |
| 日期 | YYYY-MM-DD |
| 状态 | 提议中 / 已采纳 / 已废弃 / 已被 ADR-XXXX 取代 |
| 影响范围 | 构建 / 架构分层 / 依赖 / 测试 / 发布 |

## 决策

> 一句话：我们选 X。

## 背景

> 什么问题逼着做这个选择？约束是什么（包体、启动耗时、可测性、离线要求、合规）？

## 候选方案

| 方案 | 优势 | 代价 | 关键风险 |
|---|---|---|---|
| A（选中） | | | |
| B | | | |

> 有实测或构建输出支撑的，贴命令和结果。只凭文档判断的标注「基于文档判断，未实测」。

## 理由

> 为什么是 A 而不是 B。说清权衡点，而不是只列 A 的好处。

## 后果

- 接受了什么代价：
- 引入了什么依赖 / 锁定：
- 什么条件下应该重新评估这个决策：

## 参考

-
